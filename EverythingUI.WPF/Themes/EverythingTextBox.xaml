<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:EverythingUI.WPF.Controls">

    <Style TargetType="controls:EverythingTextBox">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        <Setter Property="CaretBrush" Value="{DynamicResource PrimaryBrush}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="MinHeight" Value="40"/>
        <Setter Property="VerticalContentAlignment" Value="Top"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
        <Setter Property="TextOptions.TextRenderingMode" Value="Auto"/>
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="AcceptsReturn" Value="True"/>
        <Setter Property="AcceptsTab" Value="True"/>
        <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:EverythingTextBox">
                    <Grid>
                        <Border x:Name="outerFrame"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                BorderBrush="{DynamicResource Gray400Brush}"
                                BorderThickness="1"
                                SnapsToDevicePixels="True">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#FFFFFF" Offset="0"/>
                                    <GradientStop Color="#F8F8F8" Offset="0.5"/>
                                    <GradientStop Color="#FFFFFF" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            
                            <Border x:Name="innerShadowLayer"
                                    CornerRadius="{TemplateBinding CornerRadius}"
                                    Margin="1"
                                    SnapsToDevicePixels="True">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#FFFFFF" Offset="0"/>
                                        <GradientStop Color="#E5E5E5" Offset="0.5"/>
                                        <GradientStop Color="#FFFFFF" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                
                                <Border x:Name="contentArea"
                                        CornerRadius="4"
                                        Margin="2"
                                        Background="Transparent"
                                        SnapsToDevicePixels="True">
                                    
                                    <Grid>
                                        <ScrollViewer x:Name="PART_ContentHost"
                                                      Focusable="False"
                                                      HorizontalScrollBarVisibility="{TemplateBinding HorizontalScrollBarVisibility}"
                                                      VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}"
                                                      Margin="12,10"/>
                                        
                                        <TextBlock x:Name="placeholder"
                                                   Text="{TemplateBinding Placeholder}"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   VerticalAlignment="Top"
                                                   HorizontalAlignment="Left"
                                                   IsHitTestVisible="False"
                                                   Visibility="Collapsed"
                                                   FontSize="{TemplateBinding FontSize}"
                                                   Margin="15,12,21,0"/>
                                    </Grid>
                                </Border>
                            </Border>
                        </Border>
                        
                        <Border x:Name="glossyOverlay"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                SnapsToDevicePixels="True"
                                IsHitTestVisible="False">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Offset="0" Color="#40FFFFFF"/>
                                    <GradientStop Offset="0.5" Color="#10FFFFFF"/>
                                    <GradientStop Offset="0.5" Color="#00FFFFFF"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Text" Value="">
                            <Setter TargetName="placeholder" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="outerFrame" Property="BorderBrush" Value="{DynamicResource Gray400Brush}"/>
                            <Setter TargetName="innerShadowLayer" Property="Background">
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#FAFAFA" Offset="0"/>
                                        <GradientStop Color="#D8D8D8" Offset="0.5"/>
                                        <GradientStop Color="#FAFAFA" Offset="1"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="outerFrame" Property="BorderBrush" Value="{DynamicResource BorderFocusBrush}"/>
                            <Setter TargetName="innerShadowLayer" Property="Background">
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#F5F5F5" Offset="0"/>
                                        <GradientStop Color="#D0D0D0" Offset="0.5"/>
                                        <GradientStop Color="#F5F5F5" Offset="1"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="outerFrame" Property="Background" Value="{DynamicResource Gray100Brush}"/>
                            <Setter TargetName="outerFrame" Property="BorderBrush" Value="{DynamicResource Gray300Brush}"/>
                            <Setter TargetName="innerShadowLayer" Property="Background" Value="{DynamicResource Gray200Brush}"/>
                            <Setter TargetName="glossyOverlay" Property="Visibility" Value="Collapsed"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextDisabledBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>
